import Page from '@componenents/Page';

export default function NotFound() {
	return (
		<Page description="The page you were requesting could not be found." title="Not found">
			<div />
		</Page>
	);
}
